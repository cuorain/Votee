<div class="user-info">
  <div class="col-md-12 user-status text-center">
    <div class="row">
      <div class="col-md-3 offset-md-3 user-name">
        <% if current_user.image? %>
          <%= image_tag current_user.image.url, id: :image_preview %>
        <% else %>
          <%= image_tag '/user_images/default.jpg', id: :image_preview %>
        <% end %>
        <h1><%= current_user.name %></h1>
      </div>
      <div class="col-md-3 user-score">
        <div class="survey-stats border rounded-top-lg">
          <div class="bg-light-custom">アンケート</div>
          <div class="d-flex">
            <div class="flex-fill border-right">
              主催<br/>
              <strong><%= current_user.survey.count %></strong>
            </div>
            <div class="flex-fill">
              参加中<br/>
              <strong><%= current_user.vote.count %></strong>
            </div>
          </div>
        </div>
        <div class="follow-stats border rounded-top-lg">
          <div class="bg-light-custom">フォロー</div>
          <div class="d-flex justify-content-between">
            <div class="flex-fill border-right">
              <span> フォロー </span><br/>
              <strong><%= current_user.survey.count %></strong>
            </div>
            <div class="flex-fill">
              フォロワー<br/>
              <strong><%= current_user.vote.count %></strong>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-md-6 rounded-0 card my-survey">
      <%= render "surveys/my_surveys" %>
    </div>
    <div class="col-md-6 rounded-0 card voted-survey">
      <%= render "surveys/voted_surveys"%>
    </div>
  </div>
</div>
